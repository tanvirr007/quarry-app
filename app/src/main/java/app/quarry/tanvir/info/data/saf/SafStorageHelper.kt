package app.quarry.tanvir.info.data.saf

import android.content.Context
import android.net.Uri

class SafStorageHelper(private val context: Context) {
    // SAF Helper for USB OTG and SD Card fallbacks
}
